import 'package:flutter/material.dart';

class DashboardTableScreen extends StatelessWidget {
  const DashboardTableScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Dashboard'),
      ),
    );
  }
}